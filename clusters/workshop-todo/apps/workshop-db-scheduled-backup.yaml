apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: workshop-db-backup
  namespace: default
spec:
  schedule: "0 0 0 * * *"
  cluster:
    name: workshop-db
  backupOwnerReference: cluster
  method: barmanObjectStore
  suspend: false
